# HushhTech – Local development env example
# Copy this file to `.env.local` and fill in real values.
# NOTE: `.env.local` is git-ignored via `*.local` in .gitignore – do NOT commit real secrets.

# Supabase configuration (Vite exposes only VITE_* variables to the client)
VITE_SUPABASE_URL=https://ibsisfnjxeowvdtvgzff.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE
VITE_SUPABASE_REDIRECT_URL=http://localhost:5173/

# Anthropic Claude API key (for investor profile generation)
# Used client-side via Vite to generate AI-powered investor profiles
VITE_ANTHROPIC_API_KEY=YOUR_ANTHROPIC_API_KEY_HERE
